#!/usr/bin/env bash

set -e -u -x

# This is where you can deploy your projects.
# Deployment doesn't have to be complicated, making use of `scp` and `sftp` should get the job done!

echo "Deploying project to prod..."
echo "Finished deploying to prod!"